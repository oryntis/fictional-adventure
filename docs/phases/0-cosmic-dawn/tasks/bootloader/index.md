## Task: Bootloader (Horizon Boot)

**Component:** `horizon-boot` (Rust + Assembly) | **DDR:** DDR-011

**What it does:** Reads kernel from disk, sets up memory map, hands control to kernel. Handles GOP framebuffer, ACPI tables, memory map, UEFI exit.
